--[[ 	DUPE HUB — Moon Edition 	Cloner preserved · clean loader · unique rising-moon transition ]] task.wait(5) ----------------------------------------------------------- -- SERVICES ----------------------------------------------------------- local TweenService = game:GetService("TweenService") local RunService = game:GetService("RunService") local Players = game:GetService("Players") local UserInputService = game:GetService("UserInputService") local Workspace = game:GetService("Workspace") local ReplicatedStorage = game:GetService("ReplicatedStorage") local player = Players.LocalPlayer local LP = player local pGui = player:WaitForChild("PlayerGui") ----------------------------------------------------------- -- MOON THEME ----------------------------------------------------------- local Theme = { 	Void = Color3.fromRGB(8, 9, 13), 	Panel = Color3.fromRGB(14, 15, 21), 	PanelSoft = Color3.fromRGB(20, 22, 30), 	Moonlight = Color3.fromRGB(223, 229, 240), 	Moonbeam = Color3.fromRGB(168, 183, 214), 	Silver = Color3.fromRGB(120, 132, 158), 	Glow = Color3.fromRGB(199, 210, 235), 	DeepGlow = Color3.fromRGB(58, 66, 92), 	TrackOff = Color3.fromRGB(40, 43, 54), } local FontTitle = Enum.Font.Michroma local FontBody = Enum.Font.Nunito local LOAD_TIME = 15 local TI_SMOOTH = TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out) local TI_FADE = TweenInfo.new(0.35, Enum.EasingStyle.Sine, Enum.EasingDirection.Out) local TI_QUICK = TweenInfo.new(0.2, Enum.EasingStyle.Sine, Enum.EasingDirection.Out) local sweepGradients = {} local function makeRounded(obj, r) 	local c = Instance.new("UICorner") 	c.CornerRadius = UDim.new(0, r or 14) 	c.Parent = obj 	return c end local function applyMoonGlow(obj, thickness) 	local stroke = Instance.new("UIStroke") 	stroke.Thickness = thickness or 1.4 	stroke.Color = Theme.Glow 	stroke.Transparency = 0.35 	stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border 	local g = Instance.new("UIGradient") 	g.Color = ColorSequence.new({ 		ColorSequenceKeypoint.new(0, Theme.DeepGlow), 		ColorSequenceKeypoint.new(0.5, Theme.Moonlight), 		ColorSequenceKeypoint.new(1, Theme.DeepGlow), 	}) 	g.Parent = stroke 	stroke.Parent = obj 	table.insert(sweepGradients, g) 	return stroke end ----------------------------------------------------------- -- CLONER (unchanged logic) ----------------------------------------------------------- local function findMyPlot() 	local plots = Workspace:FindFirstChild("Plots") 	if not plots then 		warn("[Cloner] No Plots folder") 		return nil 	end 	local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") 	if not hrp then 		warn("[Cloner] No HumanoidRootPart") 		return nil 	end 	local closest, closestDist = nil, math.huge 	for _, plot in ipairs(plots:GetChildren()) do 		if plot:IsA("Model") then 			local ok, pos = pcall(function() 				return plot:GetPivot().Position 			end) 			if ok then 				local dist = (pos - hrp.Position).Magnitude 				if dist < closestDist then 					closestDist = dist 					closest = plot 				end 			end 		end 	end 	if closest then 		print("[Cloner] Using closest plot (distance " .. math.floor(closestDist) .. ")") 		return closest 	end 	warn("[Cloner] Could not find any plot") 	return nil end local function getIdleAnimation(animalName) 	local animations = ReplicatedStorage:FindFirstChild("Animations") 	if not animations then return nil end 	local animals = animations:FindFirstChild("Animals") 	if not animals then return nil end 	local animalFolder = animals:FindFirstChild(animalName) 	if not animalFolder then return nil end 	return animalFolder:FindFirstChild("Idle") end local function playAnimationOnClone(clone, animalName) 	local animTrack = getIdleAnimation(animalName) 	if not animTrack then 		print("[Cloner] No idle animation found for:", animalName) 		return 	end 	local humanoid = clone:FindFirstChildOfClass("Humanoid") 	if not humanoid then 		for _, child in ipairs(clone:GetDescendants()) do 			if child:IsA("Humanoid") then 				humanoid = child 				break 			end 		end 	end 	if not humanoid then 		warn("[Cloner] No Humanoid found in clone:", clone.Name) 		return 	end 	local success, err = pcall(function() 		local loadedAnim = humanoid:LoadAnimation(animTrack) 		loadedAnim.Looped = true 		loadedAnim:Play() 		print("[Cloner] Animation playing on:", clone.Name) 	end) 	if not success then 		warn("[Cloner] Failed to play animation on", clone.Name, "→", err) 	end end local function cloneBrainrots() 	print("========== CLONER WITH ANIMATIONS STARTED ==========") 	local myPlot = findMyPlot() 	if not myPlot then return 0 end 	local folder = Instance.new("Folder") 	folder.Name = "ClonedBrainrots_" .. math.random(1000, 9999) 	folder.Parent = Workspace 	local blacklist = { 		MainRoot = true, PlotSign = true, AnimalPodiums = true, 		Laser = true, LaserHitbox = true, InvisibleWalls = true, 		Skin = true, Purchases = true, Unlock = true, Decorations = true, 		AnimalTarget = true, StealHitbox = true, Slope = true, 		DeliveryHitbox = true, Multiplier = true, Spawn = true, 		FriendPanel = true, CashPad = true, Model = true, 		Animator = true, AnimationTrack = true, Animation = true, Animate = true, 	} 	local cloned = 0 	for _, child in ipairs(myPlot:GetChildren()) do 		if child:IsA("Model") and not blacklist[child.Name] then 			local success, err = pcall(function() 				local clone = child:Clone() 				clone.Parent = folder 				if child.PrimaryPart then 					clone:SetPrimaryPartCFrame(child:GetPrimaryPartCFrame()) 				end 				task.wait(0.1) 				playAnimationOnClone(clone, child.Name) 				cloned = cloned + 1 				print("[Cloner] Cloned with animation:", child.Name) 			end) 			if not success then 				warn("[Cloner] Failed to clone", child.Name, "→", err) 			end 		end 	end 	print("Successfully cloned:", cloned) 	if cloned == 0 then 		folder:Destroy() 	end 	return cloned end -- Run once on start local success, result = pcall(cloneBrainrots) print("[Cloner] Immediate run finished. Cloned:", success and result or 0) ----------------------------------------------------------- -- DRAGGABLE ----------------------------------------------------------- local function makeDraggable(frame, handle) 	handle = handle or frame 	local dragging, dragInput, dragStart, startPos 	handle.InputBegan:Connect(function(input) 		if input.UserInputType == Enum.UserInputType.MouseButton1 			or input.UserInputType == Enum.UserInputType.Touch then 			dragging = true 			dragStart = input.Position 			startPos = frame.Position 			input.Changed:Connect(function() 				if input.UserInputState == Enum.UserInputState.End then 					dragging = false 				end 			end) 		end 	end) 	handle.InputChanged:Connect(function(input) 		if input.UserInputType == Enum.UserInputType.MouseMovement 			or input.UserInputType == Enum.UserInputType.Touch then 			dragInput = input 		end 	end) 	UserInputService.InputChanged:Connect(function(input) 		if input == dragInput and dragging then 			local d = input.Position - dragStart 			frame.Position = UDim2.new( 				startPos.X.Scale, startPos.X.Offset + d.X, 				startPos.Y.Scale, startPos.Y.Offset + d.Y 			) 		end 	end) end ----------------------------------------------------------- -- LOADER ----------------------------------------------------------- local loaderGui = Instance.new("ScreenGui") loaderGui.Name = "DupeLoader" loaderGui.ResetOnSpawn = false loaderGui.IgnoreGuiInset = true loaderGui.Parent = pGui local dim = Instance.new("Frame") dim.Size = UDim2.fromScale(1, 1) dim.BackgroundColor3 = Color3.new(0, 0, 0) dim.BackgroundTransparency = 1 dim.BorderSizePixel = 0 dim.Parent = loaderGui local loaderFrame = Instance.new("CanvasGroup") loaderFrame.Size = UDim2.fromOffset(280, 130) loaderFrame.Position = UDim2.new(0.5, 0, 0.5, 0) loaderFrame.AnchorPoint = Vector2.new(0.5, 0.5) loaderFrame.BackgroundColor3 = Theme.Panel loaderFrame.BackgroundTransparency = 0.04 loaderFrame.BorderSizePixel = 0 loaderFrame.ClipsDescendants = true loaderFrame.GroupTransparency = 1 loaderFrame.Active = true loaderFrame.Parent = loaderGui makeRounded(loaderFrame, 16) local loaderStroke = applyMoonGlow(loaderFrame, 1.4) loaderStroke.Transparency = 0.3 -- Header bits local badge = Instance.new("Frame") badge.Size = UDim2.fromOffset(28, 28) badge.Position = UDim2.fromOffset(14, 14) badge.BackgroundColor3 = Theme.PanelSoft badge.Parent = loaderFrame makeRounded(badge, 8) applyMoonGlow(badge, 1) local badgeIcon = Instance.new("TextLabel") badgeIcon.Text = "🔱" badgeIcon.Font = FontTitle badgeIcon.TextSize = 13 badgeIcon.TextColor3 = Theme.Moonlight badgeIcon.Size = UDim2.fromScale(1, 1) badgeIcon.BackgroundTransparency = 1 badgeIcon.Parent = badge local loaderTitle = Instance.new("TextLabel") loaderTitle.Text = "DUPE HUB" loaderTitle.Font = FontTitle loaderTitle.TextSize = 14 loaderTitle.TextColor3 = Theme.Moonlight loaderTitle.Size = UDim2.new(1, -60, 0, 18) loaderTitle.Position = UDim2.fromOffset(50, 12) loaderTitle.BackgroundTransparency = 1 loaderTitle.TextXAlignment = Enum.TextXAlignment.Left loaderTitle.Parent = loaderFrame local loaderSub = Instance.new("TextLabel") loaderSub.Text = "preparing" loaderSub.Font = FontBody loaderSub.TextSize = 12 loaderSub.TextColor3 = Theme.Silver loaderSub.Size = UDim2.new(1, -60, 0, 16) loaderSub.Position = UDim2.fromOffset(50, 30) loaderSub.BackgroundTransparency = 1 loaderSub.TextXAlignment = Enum.TextXAlignment.Left loaderSub.Parent = loaderFrame local barTrack = Instance.new("Frame") barTrack.Size = UDim2.new(1, -28, 0, 3) barTrack.Position = UDim2.fromOffset(14, 78) barTrack.BackgroundColor3 = Theme.TrackOff barTrack.BorderSizePixel = 0 barTrack.Parent = loaderFrame makeRounded(barTrack, 2) local barFill = Instance.new("Frame") barFill.Size = UDim2.new(0, 0, 1, 0) barFill.BackgroundColor3 = Theme.Moonbeam barFill.BorderSizePixel = 0 barFill.Parent = barTrack makeRounded(barFill, 2) local footerHint = Instance.new("TextLabel") footerHint.Text = "moon edition" footerHint.Font = FontBody footerHint.TextSize = 11 footerHint.TextColor3 = Theme.Silver footerHint.Size = UDim2.new(1, -28, 0, 14) footerHint.Position = UDim2.new(0, 14, 1, -22) footerHint.BackgroundTransparency = 1 footerHint.TextXAlignment = Enum.TextXAlignment.Left footerHint.Parent = loaderFrame makeDraggable(loaderFrame) TweenService:Create(dim, TI_FADE, {BackgroundTransparency = 0.45}):Play() TweenService:Create(loaderFrame, TI_SMOOTH, {GroupTransparency = 0}):Play() TweenService:Create(barFill, TweenInfo.new(LOAD_TIME, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), { 	Size = UDim2.new(1, 0, 1, 0), }):Play() task.wait(LOAD_TIME) loaderSub.Text = "ready" ----------------------------------------------------------- -- UNIQUE TRANSITION: rising moonlight bar + soft bloom -- (different from Freeze's expanding ring) ----------------------------------------------------------- -- 1) Soften loader content TweenService:Create(loaderTitle, TI_FADE, {TextTransparency = 1}):Play() TweenService:Create(loaderSub, TI_FADE, {TextTransparency = 1}):Play() TweenService:Create(footerHint, TI_FADE, {TextTransparency = 1}):Play() TweenService:Create(barTrack, TI_FADE, {BackgroundTransparency = 1}):Play() TweenService:Create(barFill, TI_FADE, {BackgroundTransparency = 1}):Play() TweenService:Create(badge, TI_FADE, {BackgroundTransparency = 1}):Play() TweenService:Create(loaderStroke, TI_FADE, {Transparency = 1}):Play() -- 2) Collapse loader into a thin horizontal bar TweenService:Create(loaderFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.In), { 	Size = UDim2.fromOffset(220, 4), 	GroupTransparency = 0.15, }):Play() task.wait(0.42) -- 3) Rising light bar sweeps upward from center local riseBar = Instance.new("Frame") riseBar.AnchorPoint = Vector2.new(0.5, 0.5) riseBar.Position = UDim2.new(0.5, 0, 0.5, 0) riseBar.Size = UDim2.fromOffset(8, 4) riseBar.BackgroundColor3 = Theme.Moonlight riseBar.BackgroundTransparency = 0.15 riseBar.BorderSizePixel = 0 riseBar.Parent = loaderGui makeRounded(riseBar, 4) local riseGlow = Instance.new("Frame") riseGlow.AnchorPoint = Vector2.new(0.5, 0.5) riseGlow.Position = UDim2.new(0.5, 0, 0.5, 0) riseGlow.Size = UDim2.fromOffset(40, 20) riseGlow.BackgroundColor3 = Theme.Moonbeam riseGlow.BackgroundTransparency = 0.7 riseGlow.BorderSizePixel = 0 riseGlow.ZIndex = 0 riseGlow.Parent = loaderGui makeRounded(riseGlow, 12) -- Stretch horizontally + grow vertically like a rising beam TweenService:Create(riseBar, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), { 	Size = UDim2.fromOffset(6, 420), 	BackgroundTransparency = 0.55, }):Play() TweenService:Create(riseGlow, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), { 	Size = UDim2.fromOffset(120, 480), 	BackgroundTransparency = 1, }):Play() TweenService:Create(loaderFrame, TI_FADE, {GroupTransparency = 1}):Play() TweenService:Create(dim, TweenInfo.new(0.5, Enum.EasingStyle.Sine), { 	BackgroundTransparency = 1, }):Play() task.wait(0.38) ----------------------------------------------------------- -- MAIN PANEL ----------------------------------------------------------- local mainGui = Instance.new("ScreenGui") mainGui.Name = "DupePanel" mainGui.ResetOnSpawn = false mainGui.Parent = pGui local mainFrame = Instance.new("CanvasGroup") mainFrame.Size = UDim2.fromOffset(250, 160) mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0) mainFrame.AnchorPoint = Vector2.new(0.5, 0.5) mainFrame.BackgroundColor3 = Theme.Panel mainFrame.BackgroundTransparency = 0.04 mainFrame.BorderSizePixel = 0 mainFrame.ClipsDescendants = true mainFrame.GroupTransparency = 1 mainFrame.Active = true mainFrame.Parent = mainGui makeRounded(mainFrame, 16) local mainStroke = applyMoonGlow(mainFrame, 1.5) mainStroke.Transparency = 0.3 -- Header local header = Instance.new("Frame") header.Size = UDim2.new(1, 0, 0, 46) header.BackgroundTransparency = 1 header.Parent = mainFrame local mBadge = Instance.new("Frame") mBadge.Size = UDim2.fromOffset(28, 28) mBadge.Position = UDim2.fromOffset(12, 9) mBadge.BackgroundColor3 = Theme.PanelSoft mBadge.Parent = header makeRounded(mBadge, 8) applyMoonGlow(mBadge, 1) local mBadgeIcon = Instance.new("TextLabel") mBadgeIcon.Text = "🔱" mBadgeIcon.Font = FontTitle mBadgeIcon.TextSize = 12 mBadgeIcon.TextColor3 = Theme.Moonlight mBadgeIcon.Size = UDim2.fromScale(1, 1) mBadgeIcon.BackgroundTransparency = 1 mBadgeIcon.Parent = mBadge local mTitle = Instance.new("TextLabel") mTitle.Text = "DUPE PANEL" mTitle.Font = FontTitle mTitle.TextSize = 12 mTitle.TextColor3 = Theme.Moonlight mTitle.Size = UDim2.new(1, -100, 0, 16) mTitle.Position = UDim2.fromOffset(48, 8) mTitle.BackgroundTransparency = 1 mTitle.TextXAlignment = Enum.TextXAlignment.Left mTitle.Parent = header local statusDot = Instance.new("Frame") statusDot.Size = UDim2.fromOffset(5, 5) statusDot.Position = UDim2.fromOffset(48, 30) statusDot.BackgroundColor3 = Theme.Moonbeam statusDot.BorderSizePixel = 0 statusDot.Parent = header makeRounded(statusDot, 3) task.spawn(function() 	while statusDot and statusDot.Parent do 		TweenService:Create(statusDot, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), { 			BackgroundTransparency = 0.65, 		}):Play() 		task.wait(1.5) 		if not (statusDot and statusDot.Parent) then break end 		TweenService:Create(statusDot, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), { 			BackgroundTransparency = 0, 		}):Play() 		task.wait(1.5) 	end end) local mSub = Instance.new("TextLabel") mSub.Text = "ready when you are" mSub.Font = FontBody mSub.TextSize = 11 mSub.TextColor3 = Theme.Silver mSub.Size = UDim2.new(1, -100, 0, 14) mSub.Position = UDim2.fromOffset(58, 26) mSub.BackgroundTransparency = 1 mSub.TextXAlignment = Enum.TextXAlignment.Left mSub.Parent = header local closeBtn = Instance.new("TextButton") closeBtn.Text = "×" closeBtn.Font = FontTitle closeBtn.TextSize = 16 closeBtn.TextColor3 = Theme.Silver closeBtn.BackgroundTransparency = 1 closeBtn.Size = UDim2.fromOffset(28, 28) closeBtn.Position = UDim2.new(1, -36, 0, 9) closeBtn.AutoButtonColor = false closeBtn.Parent = header closeBtn.MouseEnter:Connect(function() 	TweenService:Create(closeBtn, TI_QUICK, {TextColor3 = Color3.fromRGB(255, 110, 110)}):Play() end) closeBtn.MouseLeave:Connect(function() 	TweenService:Create(closeBtn, TI_QUICK, {TextColor3 = Theme.Silver}):Play() end) closeBtn.MouseButton1Click:Connect(function() 	local t = TweenService:Create(mainFrame, TI_FADE, { 		GroupTransparency = 1, 		Size = UDim2.fromOffset(220, 130), 	}) 	t:Play() 	t.Completed:Wait() 	mainGui:Destroy() end) local divider = Instance.new("Frame") divider.Size = UDim2.new(1, -24, 0, 1) divider.Position = UDim2.fromOffset(12, 46) divider.BackgroundColor3 = Theme.DeepGlow divider.BackgroundTransparency = 0.4 divider.BorderSizePixel = 0 divider.Parent = mainFrame local statusLabel = Instance.new("TextLabel") statusLabel.Text = "Done (ran on start)" statusLabel.Font = FontBody statusLabel.TextSize = 12 statusLabel.TextColor3 = Theme.Silver statusLabel.Size = UDim2.new(1, -24, 0, 14) statusLabel.Position = UDim2.fromOffset(12, 54) statusLabel.BackgroundTransparency = 1 statusLabel.TextXAlignment = Enum.TextXAlignment.Left statusLabel.Parent = mainFrame -- Button local btnHolder = Instance.new("Frame") btnHolder.Size = UDim2.new(1, -24, 0, 40) btnHolder.Position = UDim2.fromOffset(12, 76) btnHolder.BackgroundTransparency = 1 btnHolder.Parent = mainFrame local btnScale = Instance.new("UIScale") btnScale.Scale = 1 btnScale.Parent = btnHolder local btn = Instance.new("TextButton") btn.Size = UDim2.fromScale(1, 1) btn.BackgroundColor3 = Theme.PanelSoft btn.BackgroundTransparency = 0.05 btn.Text = "DUPE BRAINROTS" btn.Font = FontTitle btn.TextSize = 12 btn.TextColor3 = Theme.Moonlight btn.AutoButtonColor = false btn.Parent = btnHolder makeRounded(btn, 12) local btnStroke = applyMoonGlow(btn, 1.3) btnStroke.Transparency = 0.3 local btnGlow = Instance.new("Frame") btnGlow.Size = UDim2.new(1, 12, 1, 12) btnGlow.Position = UDim2.new(0.5, 0, 0.5, 0) btnGlow.AnchorPoint = Vector2.new(0.5, 0.5) btnGlow.BackgroundColor3 = Theme.Moonbeam btnGlow.BackgroundTransparency = 1 btnGlow.ZIndex = 0 btnGlow.Parent = btnHolder makeRounded(btnGlow, 14) local footerText = Instance.new("TextLabel") footerText.Text = "1 dupe completed" footerText.Font = FontBody footerText.TextSize = 11 footerText.TextColor3 = Theme.Silver footerText.Size = UDim2.new(1, -24, 0, 14) footerText.Position = UDim2.new(0, 12, 1, -18) footerText.BackgroundTransparency = 1 footerText.TextXAlignment = Enum.TextXAlignment.Left footerText.Parent = mainFrame makeDraggable(mainFrame, header) -- Main panel entrance TweenService:Create(mainFrame, TI_SMOOTH, {GroupTransparency = 0}):Play() -- Cleanup loader after main is up task.delay(0.2, function() 	if riseBar then riseBar:Destroy() end 	if riseGlow then riseGlow:Destroy() end 	if loaderGui then loaderGui:Destroy() end end) -- Button feedback btn.MouseEnter:Connect(function() 	TweenService:Create(btn, TI_QUICK, {BackgroundColor3 = Theme.DeepGlow}):Play() 	TweenService:Create(btnGlow, TI_QUICK, {BackgroundTransparency = 0.75}):Play() end) btn.MouseLeave:Connect(function() 	TweenService:Create(btn, TI_QUICK, {BackgroundColor3 = Theme.PanelSoft}):Play() 	TweenService:Create(btnGlow, TI_QUICK, {BackgroundTransparency = 1}):Play() end) btn.MouseButton1Down:Connect(function() 	TweenService:Create(btnScale, TweenInfo.new(0.08), {Scale = 0.97}):Play() end) local function releaseBtn() 	TweenService:Create(btnScale, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), { 		Scale = 1, 	}):Play() end btn.MouseButton1Up:Connect(releaseBtn) btn.MouseLeave:Connect(releaseBtn) local duping = false local dupeCount = 1 btn.MouseButton1Click:Connect(function() 	if duping then return end 	duping = true 	statusLabel.Text = "Running" 	btn.Text = "DUPING" 	task.spawn(function() 		while duping do 			TweenService:Create(btnGlow, TweenInfo.new(0.55, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), { 				BackgroundTransparency = 0.4, 			}):Play() 			task.wait(0.55) 			if not duping then break end 			TweenService:Create(btnGlow, TweenInfo.new(0.55, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), { 				BackgroundTransparency = 0.85, 			}):Play() 			task.wait(0.55) 		end 	end) 	task.spawn(function() 		local dots = 0 		while duping do 			dots = (dots % 3) + 1 			btn.Text = "DUPING" .. string.rep(".", dots) 			task.wait(0.35) 		end 	end) 	local ok, res = pcall(cloneBrainrots) 	local amount = ok and res or 0 	duping = false 	btn.Text = "FINISHED" 	statusLabel.Text = "Done" 	if amount > 0 then 		dupeCount += 1 		footerText.Text = dupeCount .. (dupeCount == 1 and " dupe completed" or " dupes completed") 	else 		statusLabel.Text = "Failed / nothing found" 	end 	TweenService:Create(btnGlow, TI_FADE, {BackgroundTransparency = 1}):Play() 	task.wait(1.1) 	btn.Text = "DUPE BRAINROTS" 	statusLabel.Text = "Idle" end) -- Border sweep task.spawn(function() 	while true do 		local t = tick() 		for _, g in ipairs(sweepGradients) do 			if g and g.Parent then 				g.Rotation = (t * 36) % 360 			end 		end 		RunService.RenderStepped:Wait() 	end end) print("[Dupe Hub] Moon edition loaded")