--[[ 	RNG LUCK — Moon Edition 	Loader → main panel → activate countdown → toast ]] local Players = game:GetService("Players") local TweenService = game:GetService("TweenService") local UserInputService = game:GetService("UserInputService") local RunService = game:GetService("RunService") local player = Players.LocalPlayer local playerGui = player:WaitForChild("PlayerGui") local Theme = { 	Void = Color3.fromRGB(8, 9, 13), 	Panel = Color3.fromRGB(14, 15, 21), 	PanelSoft = Color3.fromRGB(20, 22, 30), 	Moonlight = Color3.fromRGB(223, 229, 240), 	Moonbeam = Color3.fromRGB(168, 183, 214), 	Silver = Color3.fromRGB(120, 132, 158), 	Glow = Color3.fromRGB(199, 210, 235), 	DeepGlow = Color3.fromRGB(58, 66, 92), } local FontTitle = Enum.Font.Michroma local FontBody = Enum.Font.Nunito local function makeRounded(o, r) 	local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0, r or 12); c.Parent = o end local sweepGradients = {} local function applyMoonGlow(o, t) 	local s = Instance.new("UIStroke") 	s.Thickness = t or 1.4; s.Color = Theme.Glow; s.Transparency = 0.25 	s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border 	local g = Instance.new("UIGradient") 	g.Color = ColorSequence.new({ 		ColorSequenceKeypoint.new(0, Theme.DeepGlow), 		ColorSequenceKeypoint.new(0.5, Theme.Moonlight), 		ColorSequenceKeypoint.new(1, Theme.DeepGlow), 	}) 	g.Parent = s; s.Parent = o; table.insert(sweepGradients, g); return s end local function tween(obj, props, time, style, dir) 	pcall(function() 		TweenService:Create(obj, TweenInfo.new(time or 0.3, style or Enum.EasingStyle.Quint, dir or Enum.EasingDirection.Out), props):Play() 	end) end local function safeParent(gui) 	if typeof(protect_gui) == "function" then pcall(protect_gui, gui) end 	local ok = false 	if typeof(gethui) == "function" then ok = pcall(function() gui.Parent = gethui() end) end 	if not ok then ok = pcall(function() gui.Parent = playerGui end) end 	if not ok then pcall(function() gui.Parent = game:GetService("CoreGui") end) end end pcall(function() 	local old = playerGui:FindFirstChild("MoonRngLuck") 	if old then old:Destroy() end end) local sg = Instance.new("ScreenGui") sg.Name = "MoonRngLuck" sg.ResetOnSpawn = false sg.IgnoreGuiInset = true safeParent(sg) ------------------------------------------------------------ -- LOAD SCREEN ------------------------------------------------------------ local loadScreen = Instance.new("Frame") loadScreen.Size = UDim2.fromScale(1, 1) loadScreen.BackgroundColor3 = Theme.Void loadScreen.BorderSizePixel = 0 loadScreen.ZIndex = 100 loadScreen.Parent = sg local loadCard = Instance.new("Frame") loadCard.Size = UDim2.fromOffset(340, 380) loadCard.Position = UDim2.new(0.5, -170, 0.5, -190) loadCard.BackgroundColor3 = Theme.Panel loadCard.BorderSizePixel = 0 loadCard.ZIndex = 101 loadCard.Parent = loadScreen makeRounded(loadCard, 16) applyMoonGlow(loadCard, 1.5) local badge = Instance.new("Frame") badge.Size = UDim2.fromOffset(72, 72) badge.Position = UDim2.new(0.5, -36, 0, 20) badge.BackgroundColor3 = Theme.PanelSoft badge.BorderSizePixel = 0 badge.ZIndex = 102 badge.Parent = loadCard makeRounded(badge, 16) applyMoonGlow(badge, 1.2) local badgeIcon = Instance.new("TextLabel") badgeIcon.Size = UDim2.fromScale(1, 1) badgeIcon.BackgroundTransparency = 1 badgeIcon.Text = "🎲" badgeIcon.TextSize = 28 badgeIcon.Font = FontTitle badgeIcon.ZIndex = 103 badgeIcon.Parent = badge local loadTitle = Instance.new("TextLabel") loadTitle.Size = UDim2.new(1, -24, 0, 28) loadTitle.Position = UDim2.fromOffset(12, 104) loadTitle.BackgroundTransparency = 1 loadTitle.Font = FontTitle loadTitle.Text = "RNG LUCK" loadTitle.TextColor3 = Theme.Moonlight loadTitle.TextSize = 18 loadTitle.ZIndex = 102 loadTitle.Parent = loadCard local loadSub = Instance.new("TextLabel") loadSub.Size = UDim2.new(1, -24, 0, 18) loadSub.Position = UDim2.fromOffset(12, 132) loadSub.BackgroundTransparency = 1 loadSub.Font = FontBody loadSub.Text = "boost your rng luck" loadSub.TextColor3 = Theme.Silver loadSub.TextSize = 13 loadSub.ZIndex = 102 loadSub.Parent = loadCard local loadStatus = Instance.new("TextLabel") loadStatus.Size = UDim2.new(1, -24, 0, 18) loadStatus.Position = UDim2.fromOffset(12, 168) loadStatus.BackgroundTransparency = 1 loadStatus.Font = FontBody loadStatus.Text = "Initializing..." loadStatus.TextColor3 = Theme.Moonbeam loadStatus.TextSize = 12 loadStatus.ZIndex = 102 loadStatus.Parent = loadCard local bigTrack = Instance.new("Frame") bigTrack.Size = UDim2.new(1, -40, 0, 10) bigTrack.Position = UDim2.fromOffset(20, 200) bigTrack.BackgroundColor3 = Theme.Dim or Theme.PanelSoft bigTrack.BackgroundColor3 = Color3.fromRGB(32, 34, 42) bigTrack.BorderSizePixel = 0 bigTrack.ZIndex = 102 bigTrack.Parent = loadCard makeRounded(bigTrack, 8) local bigFill = Instance.new("Frame") bigFill.Size = UDim2.new(0, 0, 1, 0) bigFill.BackgroundColor3 = Theme.Moonbeam bigFill.BorderSizePixel = 0 bigFill.ZIndex = 103 bigFill.Parent = bigTrack makeRounded(bigFill, 8) local pctLabel = Instance.new("TextLabel") pctLabel.Size = UDim2.new(1, -24, 0, 20) pctLabel.Position = UDim2.fromOffset(12, 218) pctLabel.BackgroundTransparency = 1 pctLabel.Font = FontTitle pctLabel.Text = "0%" pctLabel.TextColor3 = Theme.Moonbeam pctLabel.TextSize = 12 pctLabel.TextXAlignment = Enum.TextXAlignment.Right pctLabel.ZIndex = 102 pctLabel.Parent = loadCard local STEPS = { 	"Warming up RNG engine...", 	"Calibrating luck values...", 	"Syncing with game server...", 	"Applying luck multipliers...", 	"Ready to go!", } local stepLabels = {} local stepsFrame = Instance.new("Frame") stepsFrame.Size = UDim2.new(1, -40, 0, 110) stepsFrame.Position = UDim2.fromOffset(20, 250) stepsFrame.BackgroundColor3 = Theme.PanelSoft stepsFrame.BackgroundTransparency = 0.15 stepsFrame.BorderSizePixel = 0 stepsFrame.ZIndex = 102 stepsFrame.Parent = loadCard makeRounded(stepsFrame, 12) for i, txt in ipairs(STEPS) do 	local lbl = Instance.new("TextLabel") 	lbl.Size = UDim2.new(1, -16, 0, 18) 	lbl.Position = UDim2.fromOffset(8, (i - 1) * 20 + 6) 	lbl.BackgroundTransparency = 1 	lbl.Font = FontBody 	lbl.Text = txt 	lbl.TextColor3 = Theme.Silver 	lbl.TextSize = 11 	lbl.TextXAlignment = Enum.TextXAlignment.Left 	lbl.TextTransparency = 0.4 	lbl.ZIndex = 103 	lbl.Parent = stepsFrame 	stepLabels[i] = lbl end local LOAD_DURATION = 15 local function runLoadScreen(onComplete) 	local startTime = os.clock() 	local msgs = { 		[0] = "Initializing...", 		[10] = "Warming up the RNG engine...", 		[30] = "Calibrating luck multipliers...", 		[60] = "Syncing with the universe...", 		[120] = "Charging luck crystals...", 		[180] = "Almost halfway there...", 		[240] = "Final calibrations...", 		[270] = "Finishing touches...", 		[290] = "Almost ready!", 	} 	task.spawn(function() 		while true do 			local elapsed = os.clock() - startTime 			if elapsed >= LOAD_DURATION then break end 			local progress = elapsed / LOAD_DURATION 			pcall(function() 				tween(bigFill, {Size = UDim2.new(progress, 0, 1, 0)}, 0.25) 				pctLabel.Text = math.floor(progress * 100) .. "%" 				local best = msgs[0] 				for th, msg in pairs(msgs) do 					if elapsed >= th then best = msg end 				end 				loadStatus.Text = best 				local stepIdx = math.floor(progress * (#STEPS - 1)) + 1 				for i, lbl in ipairs(stepLabels) do 					if i <= stepIdx then 						lbl.TextColor3 = Theme.Moonbeam 						lbl.TextTransparency = 0 					else 						lbl.TextColor3 = Theme.Silver 						lbl.TextTransparency = 0.45 					end 				end 			end) 			task.wait(0.25) 		end 		tween(bigFill, {Size = UDim2.new(1, 0, 1, 0)}, 0.4) 		pctLabel.Text = "100%" 		loadStatus.Text = "Done! Opening panel..." 		task.wait(0.7) 		tween(loadScreen, {BackgroundTransparency = 1}, 0.45) 		tween(loadCard, {BackgroundTransparency = 1}, 0.45) 		task.wait(0.5) 		loadScreen.Visible = false 		pcall(onComplete) 	end) end ------------------------------------------------------------ -- TOAST ------------------------------------------------------------ local toast = Instance.new("Frame") toast.Size = UDim2.fromOffset(300, 64) toast.Position = UDim2.new(0.5, -150, 0, -80) toast.BackgroundColor3 = Theme.Panel toast.BorderSizePixel = 0 toast.ZIndex = 40 toast.Parent = sg makeRounded(toast, 14) applyMoonGlow(toast, 1.3) local toastTitle = Instance.new("TextLabel") toastTitle.Size = UDim2.new(1, -20, 0, 24) toastTitle.Position = UDim2.fromOffset(12, 10) toastTitle.BackgroundTransparency = 1 toastTitle.Font = FontTitle toastTitle.Text = "Done Activating!" toastTitle.TextColor3 = Theme.Moonlight toastTitle.TextSize = 13 toastTitle.TextXAlignment = Enum.TextXAlignment.Left toastTitle.ZIndex = 41 toastTitle.Parent = toast local toastSub = Instance.new("TextLabel") toastSub.Size = UDim2.new(1, -20, 0, 18) toastSub.Position = UDim2.fromOffset(12, 34) toastSub.BackgroundTransparency = 1 toastSub.Font = FontBody toastSub.Text = "RNG Luck is now active" toastSub.TextColor3 = Theme.Moonbeam toastSub.TextSize = 12 toastSub.TextXAlignment = Enum.TextXAlignment.Left toastSub.ZIndex = 41 toastSub.Parent = toast local function showToast() 	tween(toast, {Position = UDim2.new(0.5, -150, 0, 18)}, 0.45, Enum.EasingStyle.Quint) 	task.delay(3.5, function() 		tween(toast, {Position = UDim2.new(0.5, -150, 0, -80)}, 0.4) 	end) end ------------------------------------------------------------ -- MAIN PANEL ------------------------------------------------------------ local main = Instance.new("Frame") main.Size = UDim2.fromOffset(300, 210) main.Position = UDim2.new(0.5, -150, 0.5, -105) main.BackgroundColor3 = Theme.Panel main.BorderSizePixel = 0 main.ClipsDescendants = true main.Visible = false main.Parent = sg makeRounded(main, 14) applyMoonGlow(main, 1.5) local titleBar = Instance.new("Frame") titleBar.Size = UDim2.new(1, 0, 0, 56) titleBar.BackgroundTransparency = 1 titleBar.Parent = main local titleText = Instance.new("TextLabel") titleText.Size = UDim2.new(1, -20, 0, 22) titleText.Position = UDim2.fromOffset(14, 10) titleText.BackgroundTransparency = 1 titleText.Font = FontTitle titleText.Text = "RNG LUCK" titleText.TextColor3 = Theme.Moonlight titleText.TextSize = 14 titleText.TextXAlignment = Enum.TextXAlignment.Left titleText.Parent = titleBar local subtitleText = Instance.new("TextLabel") subtitleText.Size = UDim2.new(1, -20, 0, 16) subtitleText.Position = UDim2.fromOffset(14, 32) subtitleText.BackgroundTransparency = 1 subtitleText.Font = FontBody subtitleText.Text = "boost your rng luck instantly" subtitleText.TextColor3 = Theme.Silver subtitleText.TextSize = 11 subtitleText.TextXAlignment = Enum.TextXAlignment.Left subtitleText.Parent = titleBar local divider = Instance.new("Frame") divider.Size = UDim2.new(1, -28, 0, 1) divider.Position = UDim2.fromOffset(14, 58) divider.BackgroundColor3 = Theme.DeepGlow divider.BackgroundTransparency = 0.4 divider.BorderSizePixel = 0 divider.Parent = main local countFrame = Instance.new("Frame") countFrame.Size = UDim2.new(1, -28, 0, 34) countFrame.Position = UDim2.fromOffset(14, 72) countFrame.BackgroundColor3 = Theme.PanelSoft countFrame.BorderSizePixel = 0 countFrame.Visible = false countFrame.Parent = main makeRounded(countFrame, 10) local cTrack = Instance.new("Frame") cTrack.Size = UDim2.new(1, -16, 0, 5) cTrack.Position = UDim2.new(0, 8, 1, -10) cTrack.BackgroundColor3 = Color3.fromRGB(32, 34, 42) cTrack.BorderSizePixel = 0 cTrack.Parent = countFrame makeRounded(cTrack, 4) local cFill = Instance.new("Frame") cFill.Size = UDim2.new(0, 0, 1, 0) cFill.BackgroundColor3 = Theme.Moonbeam cFill.BorderSizePixel = 0 cFill.Parent = cTrack makeRounded(cFill, 4) local cLabel = Instance.new("TextLabel") cLabel.Size = UDim2.new(1, -16, 0, 18) cLabel.Position = UDim2.fromOffset(8, 4) cLabel.BackgroundTransparency = 1 cLabel.Font = FontBody cLabel.Text = "Activating in 5s..." cLabel.TextColor3 = Theme.Moonbeam cLabel.TextSize = 12 cLabel.TextXAlignment = Enum.TextXAlignment.Left cLabel.Parent = countFrame local btn = Instance.new("TextButton") btn.Size = UDim2.new(1, -28, 0, 72) btn.Position = UDim2.fromOffset(14, 80) btn.BackgroundColor3 = Theme.PanelSoft btn.Text = "ACTIVATE" btn.Font = FontTitle btn.TextSize = 16 btn.TextColor3 = Theme.Moonlight btn.AutoButtonColor = false btn.Parent = main makeRounded(btn, 12) local btnStroke = applyMoonGlow(btn, 1.3) local hintLabel = Instance.new("TextLabel") hintLabel.Size = UDim2.new(1, -28, 0, 16) hintLabel.Position = UDim2.fromOffset(14, 164) hintLabel.BackgroundTransparency = 1 hintLabel.Font = FontBody hintLabel.Text = "drag header · one-time use" hintLabel.TextColor3 = Theme.Silver hintLabel.TextSize = 10 hintLabel.Parent = main -- Drag local dragging, dragStart, startPos titleBar.InputBegan:Connect(function(input) 	if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end 	dragging = true 	dragStart = input.Position 	startPos = main.Position 	input.Changed:Connect(function() 		if input.UserInputState == Enum.UserInputState.End then dragging = false end 	end) end) UserInputService.InputChanged:Connect(function(input) 	if not dragging then return end 	if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end 	local d = input.Position - dragStart 	main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y) end) btn.MouseEnter:Connect(function() 	tween(btn, {BackgroundColor3 = Theme.DeepGlow}, 0.2) end) btn.MouseLeave:Connect(function() 	tween(btn, {BackgroundColor3 = Theme.PanelSoft}, 0.2) end) local isCooling = false btn.Activated:Connect(function() 	if isCooling then return end 	isCooling = true 	local DURATION = 5 	local endTime = os.clock() + DURATION 	tween(btn, {Size = UDim2.new(1, -28, 0, 42), Position = UDim2.fromOffset(14, 118)}, 0.3) 	btn.Text = "Activating..." 	btn.TextSize = 13 	countFrame.Visible = true 	task.spawn(function() 		while true do 			local remaining = math.ceil(endTime - os.clock()) 			if remaining <= 0 then break end 			local progress = (DURATION - remaining) / DURATION 			cLabel.Text = "Activating in " .. remaining .. "s..." 			tween(cFill, {Size = UDim2.new(progress, 0, 1, 0)}, 0.12) 			task.wait(0.1) 		end 		tween(cFill, {Size = UDim2.new(1, 0, 1, 0)}, 0.2) 		cLabel.Text = "Done" 		task.wait(0.3) 		countFrame.Visible = false 		tween(btn, { 			Size = UDim2.new(1, -28, 0, 72), 			Position = UDim2.fromOffset(14, 80), 			BackgroundColor3 = Color3.fromRGB(40, 80, 60), 		}, 0.35, Enum.EasingStyle.Back) 		btn.Text = "ACTIVATED" 		btn.TextSize = 16 		btn.TextColor3 = Theme.Moonlight 		showToast() 	end) end) runLoadScreen(function() 	main.Visible = true 	main.Position = UDim2.new(0.5, -150, 0.6, -105) 	tween(main, {Position = UDim2.new(0.5, -150, 0.5, -105)}, 0.5, Enum.EasingStyle.Quint) end) RunService.RenderStepped:Connect(function() 	local t = tick() 	for _, g in ipairs(sweepGradients) do 		if g and g.Parent then g.Rotation = (t * 32) % 360 end 	end end) print("[Moon RNG Luck] ready")